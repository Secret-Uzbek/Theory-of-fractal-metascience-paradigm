name: Release and Publish

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create release bundle
        run: |
          zip -r theory-of-fractal-metascience-paradigm-${GITHUB_REF_NAME}.zip . \
            -x ".git/*" ".github/*"

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: theory-of-fractal-metascience-paradigm-${{ github.ref_name }}.zip
